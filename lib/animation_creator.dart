import 'package:flutter/material.dart';

class AnimationCreator extends StatelessWidget {
  const AnimationCreator({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("AnimationCreator")),
      body: const Center(
        child: Text("bruh!"),
      ),
    );
  }
}
